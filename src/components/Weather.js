import React, { Component } from 'react';

class Weather extends Component {
  constructor() {
    super()
    this.state = {
      // timer counting when last time weather was updated?  or something more simple?
    }
  }

  render() {
    return (
      <p>Weather</p>
    )
  }
}

export default Weather;